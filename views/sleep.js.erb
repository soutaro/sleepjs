(function () {
  Sleep = {};

  Sleep.url = "//<%= host %>/delay/";

  Sleep.sleep = function (seconds) {
    var xhr = new XMLHttpRequest();
    xhr.open('GET', Sleep.url + seconds, false);
    xhr.send();
    return true;
  }

  window.Sleep = Sleep;
})();